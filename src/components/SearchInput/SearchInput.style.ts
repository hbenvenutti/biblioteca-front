import styled from "styled-components";

export const Container = styled.input`
/* Para compensar a outline */
margin-top: 2px; 

box-sizing: initial;
height: 2rem;
outline: 2px solid var(--pink-800);
border: 4px solid var(--gray-800);
border-radius: 8px;
`
